# 智能作业 H5 · znzy.lhyun.net

upstream znzy_api {
    server api:4000;
    keepalive 16;
}

server {
    listen 80;
    server_name znzy.lhyun.net;

    client_max_body_size 25m;

    # 若已配置 HTTPS，取消下方注释并配置证书路径
    # return 301 https://$host$request_uri;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://znzy_api/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /uploads/ {
        proxy_pass http://znzy_api/uploads/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location /embed/ {
        proxy_pass http://znzy_api/embed/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# HTTPS（证书就绪后启用整段 server）
# server {
#     listen 443 ssl http2;
#     server_name znzy.lhyun.net;
#
#     ssl_certificate     /etc/nginx/ssl/znzy.lhyun.net/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/znzy.lhyun.net/privkey.pem;
#
#     client_max_body_size 25m;
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location /api/ {
#         proxy_pass http://znzy_api/api/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
#
#     location /uploads/ {
#         proxy_pass http://znzy_api/uploads/;
#     }
#
#     location /embed/ {
#         proxy_pass http://znzy_api/embed/;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
